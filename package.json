{
  "name": "note",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package": "electron-packager . SimpleNote --platform=win32 --arch=x64 --icon=computer.ico --out=./out --asar --app-version=0.0.1 --overwrite"
  },
  "author": "Jerry",
  "license": "ISC",
  "devDependencies": {
    "electron": "^10.1.3"
  },
  "dependencies": {
    "node-uuid": "^1.4.8"
  }
}